{
  "name": "planning-poker",
  "version": "1.0.0",
  "description": "Socket based planning poker",
  "main": "index.js",
  "scripts": {
    "start": "node src/app.js",
    "postinstall": "npm run build",
    "prebuild": "npm run build:fe",
    "build": "tsc",
    "build:fe": "rm -r dist |true && cd frontend && pwd && npm install && npm run build && mv ./dist ../",
    "dev": "nodemon -e .ts --exec ts-node src/app.ts",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/pedsm/planning-poker.git"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/pedsm/planning-poker/issues"
  },
  "homepage": "https://github.com/pedsm/planning-poker#readme",
  "dependencies": {
    "@types/express": "^4.17.13",
    "@types/node": "^16.7.10",
    "cors": "^2.8.5",
    "dotenv": "^8.2.0",
    "express": "^4.17.1",
    "socket.io": "^4.0.0"
  },
  "devDependencies": {
    "eslint": "^7.32.0",
    "eslint-plugin-vue": "^7.0.0-beta.4",
    "nodemon": "^2.0.4",
    "ts-node": "^10.2.1",
    "typescript": "^4.4.2"
  },
  "engines": {
    "node": "14.x"
  }
}
